node_modules/
dist/
logs/

# Runtime data: SQLite db, cookie Zalo (credential!), file tải về
data/

# Env thật không bao giờ commit
.env
.env.*
!.env.example
!.env.production.example

# Cấu hình account thật (chứa user id, persona riêng)
config/accounts.json

# QR login tạm
qr*.png

# Cấu hình MCP server của máy dev (đường dẫn tuyệt đối, có thể chứa token)
.mcp.json

# Docker runtime
docker-compose.override.yml

# Temporary logs, backups, OS metadata, scratch patches
*.log
*.bak
.DS_Store
*/.DS_Store
patch_*.py
patch_*.js
test-label.ts
test_parse.ts
9router-search.txt
cleaned_persona.txt
